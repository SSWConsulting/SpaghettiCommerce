using Microsoft.AspNetCore.Mvc;

namespace RubeGoldbergCommerce.Controllers;

[ApiController]
[Route("[controller]")]
public class CartController : ControllerBase
{
    // Get Cart (ID)

    // Add item to cart (product ID, quantity, cart ID)

    // Remove item from cart (product ID, quantity, cart ID)
}
